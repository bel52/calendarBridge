# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/

# macOS
.DS_Store

# Virtualenv
.venv/

# App data / runtime artifacts
outbox/
logs/
*.log

# Google credentials/tokens (keep ONLY local)
credentials.json
token.json

# Optional local state files (uncomment if you don't want them in git)
# sync_state.json
# synced_events.json

# LaunchAgent logs (paths are set inside the plist)
launchd.out
launchd.err

# Local backups
*.bak*
sync_state.json
sync_state_backup.json
.idea/
sync_state.json
sync_state_backup.json
.idea/
outbox/*.ics
